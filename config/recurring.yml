# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

default: &default
  request_queue:
    class: RequestQueueJob
    queue: default
    schedule: every 5 minutes
  cleanup_temp_files:
    class: CleanupTempFilesJob
    queue: default
    schedule: every hour
  upload_recovery:
    class: UploadRecoveryJob
    queue: default
    schedule: every 5 minutes
  direct_download_recovery:
    class: DirectDownloadRecoveryJob
    queue: default
    schedule: every 5 minutes
  post_processing_recovery:
    class: PostProcessingRecoveryJob
    queue: default
    schedule: every 5 minutes
  book_metadata_backfill:
    class: BookMetadataBackfillJob
    queue: default
    schedule: at 3am every day
  update_check:
    class: UpdateCheckJob
    queue: default
    schedule: every 6 hours
  owned_library_automation:
    class: OwnedLibraryAutomationJob
    queue: default
    schedule: every 5 minutes
  health_check:
    class: HealthCheckJob
    queue: default
    args:
      - scheduled: true
    schedule: every minute
  audiobookshelf_library_sync:
    class: AudiobookshelfLibrarySyncJob
    queue: default
    args:
      - scheduled: true
    schedule: every minute

development:
  <<: *default

test:
  # Recurring jobs disabled in test environment

production:
  <<: *default
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12
